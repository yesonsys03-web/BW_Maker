# PSD Line Export — App UI (Plan B) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 완성된 Python 엔진(JSON-RPC 사이드카) 위에 Tauri v2 + React UI를 얹어, `npm run tauri dev`로 실행되는 데스크톱 앱을 완성한다. (PyInstaller 패키징·배포는 Plan C — 본 계획 범위 아님)

**Architecture:** Rust 쉘이 개발 모드에서 `uv run python -m psd_engine`을 자식 프로세스로 spawn하고 줄 단위 JSON-RPC를 중계한다(요청 id 상관, progress 이벤트는 Tauri 이벤트로 방출). React UI는 파일 패널/레이어 트리/미리보기/프리셋/작업 히스토리/내보내기/배치를 제공한다. 편집은 비파괴 operation list로 쌓이고 내보내기 시 엔진이 적용한다.

**Tech Stack:** Tauri v2 (Rust), React 18 + TypeScript + Vite, @tauri-apps/plugin-dialog, @tauri-apps/plugin-fs, vitest. 엔진: 기존 engine/ (uv, 52 tests green).

**Spec:** `docs/superpowers/specs/2026-07-30-psd-layer-tool-design.md` (3·4·5·6·7절)

## Global Constraints

- 저장소 루트 /Users/usabatch/coding/psd_line_export. 프론트는 루트의 `src/`+`package.json`, Rust는 `src-tauri/`, 엔진은 `engine/` (수정 금지 — 엔진 변경이 필요하면 BLOCKED 보고).
- 엔진 프로토콜(변경 불가): 요청 `{id, method, params}` 한 줄 → 응답 `{id, result}` | `{id, error:{message, traceback}}`; 이벤트 `{event:"progress", stage, current, total, path?}`. 허용 method: open_psd, close_session, render_thumbnails, render_preview, apply_preset, export_psd, batch_run.
- 엔진 반환 형태: open_psd→{sessionId,width,height,colorMode,depth,tree}; tree 노드 {id,name,kind,visible,blendMode,opacity,bbox,hasMask,path,children?}; export_psd(sessionId,includedIds,operations,naming,outputPath,embedPreview,overwrite,verify)→{outputPath,layerCount,verification}; batch_run(paths,preset,outputDir,overwrite)→{results:[...]}.
- 에러 fallback·흡수 금지: 엔진 error의 traceback 전문을 UI 에러 패널에 그대로 표시한다.
- 원본 PSD 절대 수정 금지. 저장 위치는 항상 사용자가 선택(단일: 저장 다이얼로그, 배치: 출력 폴더 1회 + 동명 파일 사전 확인).
- UI 표시 텍스트는 한국어.
- 커밋 메시지 끝에 항상:
  `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>` 와
  `Claude-Session: https://claude.ai/code/session_015qxsoeck1KQRvYE2KqBxK7`
- 프론트 명령은 저장소 루트에서: `npm install`, `npm run test` (vitest run), `npm run tauri dev`. Rust 검증: `cd src-tauri && cargo build`.
- `npm run tauri dev`는 창을 띄우는 장수명 명령 — 자동화 단계에서는 빌드 확인용으로 `cargo build`와 `npm run build`(vite build)를 사용하고, 실제 창 확인은 컨트롤러가 수행한다.

---

### Task 1: Tauri v2 + React 스캐폴드

**Files:**
- Create: `package.json`, `vite.config.ts`, `tsconfig.json`, `index.html`, `src/main.tsx`, `src/App.tsx`, `src/App.css`
- Create: `src-tauri/` (Cargo.toml, tauri.conf.json, src/main.rs, src/lib.rs, build.rs, icons/, capabilities/)
- Modify: `.gitignore` (루트 — node_modules/, dist/, src-tauri/target/)

**Interfaces:**
- Produces: `npm run tauri dev`가 빈 창을 띄우는 동작하는 스캐폴드. 이후 태스크가 사용할 플러그인(dialog, fs) 설치·등록 완료.

- [ ] **Step 1: 스캐폴드 생성**

루트가 비어있지 않으므로(docs/, engine/) 임시 폴더에 생성 후 이동한다:

```bash
cd /Users/usabatch/coding/psd_line_export
npm create tauri-app@latest _scaffold -- --yes --name psd_line_export --identifier com.yeson.psdlineexport --template react-ts --manager npm
# 생성 결과물을 루트로 이동 (기존 docs/, engine/과 충돌 없음)
rsync -a _scaffold/ ./ --exclude node_modules
rm -rf _scaffold
npm install
```

`--yes`/플래그가 버전에 따라 다르면 `npm create tauri-app@latest -- --help`로 확인해 동등 옵션 사용. 생성 후 `src-tauri/tauri.conf.json`에서 확인/수정:
- `productName: "PSD Line Export"`, 창 기본 크기 width 1440 / height 900, title "PSD Line Export"
- `build.beforeDevCommand: "npm run dev"`, `devUrl` vite 기본(1420)

- [ ] **Step 2: 플러그인 설치**

```bash
npm run tauri add dialog
npm run tauri add fs
```

(각각 npm 패키지 @tauri-apps/plugin-dialog/-fs + Cargo crate + lib.rs 등록 + capabilities 권한을 자동 구성. 자동 구성이 안 되면 Tauri v2 문서대로 수동: Cargo.toml에 tauri-plugin-dialog/fs, lib.rs에 `.plugin(tauri_plugin_dialog::init())` `.plugin(tauri_plugin_fs::init())`, capabilities/default.json에 `dialog:default`, `fs:default` + `fs:allow-appdata-write-recursive`.)

- [ ] **Step 3: vitest 설정**

```bash
npm install -D vitest
```

package.json scripts에 `"test": "vitest run"` 추가. `src/lib/smoke.test.ts`:

```ts
import { expect, test } from "vitest";
test("vitest works", () => { expect(1 + 1).toBe(2); });
```

- [ ] **Step 4: 빌드 검증**

Run: `npm run build && (cd src-tauri && cargo build)` 그리고 `npm run test`
Expected: 모두 성공. (창 확인은 컨트롤러가 수행)

- [ ] **Step 5: 커밋**

```bash
git add -A
git commit -m "feat(app): Tauri v2 + React scaffold with dialog/fs plugins"
```

---

### Task 2: Rust 엔진 브리지

**Files:**
- Create: `src-tauri/src/engine.rs`
- Modify: `src-tauri/src/lib.rs` (state 등록, invoke_handler, setup에서 spawn)
- Modify: `src-tauri/Cargo.toml` (base64 = "0.22", serde_json, tokio는 tauri 동봉)

**Interfaces:**
- Produces (Tauri commands, 이후 태스크의 TS가 호출):
  - `engine_request(method: String, params: Value) -> Result<Value, Value>` — 성공 시 엔진 result, 실패 시 엔진 error 객체({message, traceback}) 그대로.
  - `read_file_b64(path: String) -> Result<String, String>` — PNG 등 파일을 base64로.
  - `restart_engine() -> Result<(), String>`
- Produces (Tauri events → webview): `"engine-event"` (progress 등 id 없는 엔진 라인 그대로), `"engine-dead"` (자식 프로세스 종료 시).

- [ ] **Step 1: engine.rs 구현**

```rust
use std::collections::HashMap;
use std::io::{BufRead, BufReader, Write};
use std::process::{Child, ChildStdin, Command, Stdio};
use std::sync::atomic::{AtomicU64, Ordering};
use std::sync::{Arc, Mutex};

use serde_json::{json, Value};
use tauri::{AppHandle, Emitter, Manager, State};
use tokio::sync::oneshot;

pub struct EngineState {
    proc: Mutex<Option<EngineProc>>,
    pending: Arc<Mutex<HashMap<u64, oneshot::Sender<Value>>>>,
    next_id: AtomicU64,
}

struct EngineProc {
    child: Child,
    stdin: ChildStdin,
}

impl Default for EngineState {
    fn default() -> Self {
        Self {
            proc: Mutex::new(None),
            pending: Arc::new(Mutex::new(HashMap::new())),
            next_id: AtomicU64::new(1),
        }
    }
}

fn engine_command() -> Command {
    // dev: 저장소의 engine/ 프로젝트를 uv로 실행. (릴리스 사이드카는 Plan C)
    let engine_dir = std::path::Path::new(env!("CARGO_MANIFEST_DIR")).join("../engine");
    let mut c = Command::new("uv");
    c.args(["run", "python", "-m", "psd_engine"])
        .current_dir(engine_dir)
        .stdin(Stdio::piped())
        .stdout(Stdio::piped());
    c
}

pub fn spawn_engine(app: &AppHandle) -> Result<(), String> {
    let state: State<EngineState> = app.state();
    let mut child = engine_command().spawn().map_err(|e| e.to_string())?;
    let stdin = child.stdin.take().ok_or("no stdin")?;
    let stdout = child.stdout.take().ok_or("no stdout")?;

    let pending = state.pending.clone();
    let app_handle = app.clone();
    std::thread::spawn(move || {
        let reader = BufReader::new(stdout);
        for line in reader.lines() {
            let Ok(line) = line else { break };
            if line.trim().is_empty() {
                continue;
            }
            let Ok(v) = serde_json::from_str::<Value>(&line) else { continue };
            match v.get("id").and_then(Value::as_u64) {
                Some(id) => {
                    if let Some(tx) = pending.lock().unwrap().remove(&id) {
                        let _ = tx.send(v);
                    }
                }
                None => {
                    let _ = app_handle.emit("engine-event", v);
                }
            }
        }
        // EOF: 프로세스 사망. 대기 중 요청 전부 실패 처리 후 알림.
        let mut p = pending.lock().unwrap();
        for (_, tx) in p.drain() {
            let _ = tx.send(json!({"error": {"message": "engine process died", "traceback": ""}}));
        }
        let _ = app_handle.emit("engine-dead", ());
    });

    *state.proc.lock().unwrap() = Some(EngineProc { child, stdin });
    Ok(())
}

#[tauri::command]
pub async fn engine_request(
    state: State<'_, EngineState>,
    method: String,
    params: Value,
) -> Result<Value, Value> {
    let id = state.next_id.fetch_add(1, Ordering::SeqCst);
    let (tx, rx) = oneshot::channel();
    state.pending.lock().unwrap().insert(id, tx);

    {
        let mut guard = state.proc.lock().unwrap();
        let proc = guard
            .as_mut()
            .ok_or_else(|| json!({"message": "engine not running", "traceback": ""}))?;
        let line = json!({"id": id, "method": method, "params": params}).to_string();
        writeln!(proc.stdin, "{line}").and_then(|_| proc.stdin.flush()).map_err(|e| {
            json!({"message": format!("engine write failed: {e}"), "traceback": ""})
        })?;
    }

    let resp = rx
        .await
        .map_err(|_| json!({"message": "engine died before responding", "traceback": ""}))?;
    if let Some(err) = resp.get("error") {
        return Err(err.clone());
    }
    Ok(resp.get("result").cloned().unwrap_or(Value::Null))
}

#[tauri::command]
pub fn read_file_b64(path: String) -> Result<String, String> {
    use base64::Engine as _;
    std::fs::read(&path)
        .map(|b| base64::engine::general_purpose::STANDARD.encode(b))
        .map_err(|e| format!("{path}: {e}"))
}

#[tauri::command]
pub fn restart_engine(app: AppHandle, state: State<'_, EngineState>) -> Result<(), String> {
    if let Some(mut p) = state.proc.lock().unwrap().take() {
        let _ = p.child.kill();
        let _ = p.child.wait();
    }
    spawn_engine(&app)
}
```

lib.rs: `.manage(engine::EngineState::default())`, setup에서 `engine::spawn_engine(app.handle())?`, invoke_handler에 세 command 등록. 앱 종료 시 자식 정리: `.on_window_event`에서 Destroyed 시 child kill (또는 RunEvent::Exit).

- [ ] **Step 2: 라인 라우팅 순수 함수 + Rust 테스트**

reader 스레드의 분기(id 유무)를 순수 함수로 추출해 단위 테스트:

```rust
pub enum Routed { Response(u64, Value), Event(Value), Skip }

pub fn route_line(line: &str) -> Routed {
    if line.trim().is_empty() { return Routed::Skip; }
    let Ok(v) = serde_json::from_str::<Value>(line) else { return Routed::Skip; };
    match v.get("id").and_then(Value::as_u64) {
        Some(id) => Routed::Response(id, v),
        None => Routed::Event(v),
    }
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn routes_response_event_skip() {
        assert!(matches!(route_line(r#"{"id":3,"result":{}}"#), Routed::Response(3, _)));
        assert!(matches!(route_line(r#"{"event":"progress","stage":"compose"}"#), Routed::Event(_)));
        assert!(matches!(route_line(r#"{"id":null,"error":{"message":"x"}}"#), Routed::Event(_)));
        assert!(matches!(route_line(""), Routed::Skip));
        assert!(matches!(route_line("not json"), Routed::Skip));
    }
}
```

reader 스레드는 route_line을 사용하도록 리팩터.

- [ ] **Step 3: 검증**

Run: `cd src-tauri && cargo build && cargo test`
Expected: 빌드 성공, route_line 테스트 통과.

- [ ] **Step 4: 커밋**

```bash
git add src-tauri/
git commit -m "feat(app): Rust engine bridge (spawn, request correlation, events)"
```

---

### Task 3: TS 엔진 클라이언트 + 타입

**Files:**
- Create: `src/lib/engine.ts`, `src/lib/types.ts`
- Test: `src/lib/engine.test.ts`

**Interfaces:**
- Produces (`src/lib/types.ts`):

```ts
export interface TreeNode {
  id: number; name: string; kind: string; visible: boolean;
  blendMode: string; opacity: number; bbox: [number, number, number, number];
  hasMask: boolean; path: string[]; children?: TreeNode[];
}
export interface OpenResult { sessionId: number; width: number; height: number; colorMode: string; depth: number; tree: TreeNode[]; }
export type Operation =
  | { op: "exclude"; layerIds: number[] }
  | { op: "rename"; layerId: number; name: string }
  | { op: "merge"; layerIds: number[]; name: string }
  | { op: "flatten"; name: string }
  | { op: "reorder"; layerId: number; aboveId: number | null };
export interface Preset {
  name: string;
  include: { type: "contains" | "regex"; value: string; caseSensitive: boolean };
  excludeGroupPrefixes: string[]; matchGroups: boolean; includeHidden: boolean;
  merge: "none" | "all" | "perGroup"; naming: "pathPrefix" | "original";
  outputSuffix: string; embedPreview: boolean;
}
export interface EngineError { message: string; traceback: string; }
export interface Verification { ok: boolean; canvasOk: boolean; layerCountOk: boolean; expectedLayers: number; actualLayers: number; layers: { name: string; nameOk: boolean; pixelChecked: boolean; pixelOk: boolean | null }[]; }
export interface ExportResult { outputPath: string; layerCount: number; verification?: Verification; }
export interface BatchItemResult { path: string; ok: boolean; outputPath?: string; layerCount?: number; verification?: Verification; error?: EngineError; }
```

- Produces (`src/lib/engine.ts`): `callEngine(method, params)` — `invoke("engine_request", {method, params})` 래퍼, 실패 시 `EngineError` throw(엔진 error 객체 그대로). 편의 함수 `openPsd(path)`, `applyPreset(sessionId, preset)`, `renderPreview(sessionId, visibleLayerIds, maxSize)`, `renderThumbnails(...)`, `exportPsd(...)`, `batchRun(...)`, `closeSession(...)`, `loadPngDataUrl(path)` (read_file_b64 → `data:image/png;base64,...`), `onEngineEvent(cb)`/`onEngineDead(cb)` (plugin event listen 래퍼).

- [ ] **Step 1: 실패하는 테스트 작성** — `@tauri-apps/api`의 invoke를 vi.mock으로 대체:

```ts
import { beforeEach, expect, test, vi } from "vitest";

const invokeMock = vi.fn();
vi.mock("@tauri-apps/api/core", () => ({ invoke: (...a: unknown[]) => invokeMock(...a) }));

import { callEngine, loadPngDataUrl } from "./engine";

beforeEach(() => invokeMock.mockReset());

test("callEngine passes method/params and returns result", async () => {
  invokeMock.mockResolvedValue({ sessionId: 1 });
  const r = await callEngine("open_psd", { path: "/a.psd" });
  expect(invokeMock).toHaveBeenCalledWith("engine_request", { method: "open_psd", params: { path: "/a.psd" } });
  expect(r).toEqual({ sessionId: 1 });
});

test("callEngine surfaces engine error with traceback", async () => {
  invokeMock.mockRejectedValue({ message: "boom", traceback: "Traceback ..." });
  await expect(callEngine("open_psd", { path: "x" })).rejects.toMatchObject({ message: "boom", traceback: expect.stringContaining("Traceback") });
});

test("loadPngDataUrl builds data url", async () => {
  invokeMock.mockResolvedValue("QUJD");
  await expect(loadPngDataUrl("/tmp/p.png")).resolves.toBe("data:image/png;base64,QUJD");
});
```

- [ ] **Step 2: 실패 확인** — `npm run test` → 모듈 없음으로 FAIL
- [ ] **Step 3: 구현** — 위 인터페이스 그대로. 오류는 절대 삼키지 않는다(catch 후 재-throw 금지 패턴 불필요, 그대로 전파).
- [ ] **Step 4: 통과 확인** — `npm run test`
- [ ] **Step 5: 커밋** — `feat(app): typed engine client`

---

### Task 4: Ops 리듀서 (entries 미러 + undo)

**Files:**
- Create: `src/lib/opsReducer.ts`
- Test: `src/lib/opsReducer.test.ts`

**Interfaces:**
- Produces: UI 상태의 단일 소스. 엔진 build_export_plan과 **동일한 의미론**의 클라이언트 미러(내보내기 목록 표시·reorder UI용). 실제 내보내기는 항상 엔진이 수행한다.

```ts
export interface Entry { entryId: number; sourceIds: number[]; name: string | null; }
export interface OpsState {
  includedIds: number[];      // 체크박스 (정렬된 리프 id, 아래→위)
  previewHiddenIds: number[]; // 눈 아이콘 off (미리보기 전용, 내보내기와 무관)
  ops: Operation[];           // exclude 제외 (체크박스가 대신함): merge/rename/reorder/flatten
  entries: Entry[];           // includedIds+ops로부터 계산된 현재 내보내기 목록 (아래→위)
}
export type OpsAction =
  | { type: "reset"; includedIds: number[] }
  | { type: "setIncluded"; includedIds: number[] }
  | { type: "togglePreview"; layerId: number }
  | { type: "pushOp"; op: Operation }   // 적용 실패(잘못된 참조 등) 시 throw — UI가 잡아 표시
  | { type: "undo" };                   // 마지막 op 제거 후 entries 재계산
export function opsReducer(state: OpsState, action: OpsAction): OpsState;
export function buildEntries(includedIds: number[], ops: Operation[]): Entry[]; // 엔진 미러
```

buildEntries 의미론(엔진 ops.py와 동일): merge는 최상단 소스 위치에 삽입, sourceIds 아래→위 유지, merge 결과 entryId는 -1부터 감소, 재병합 가능; flatten은 전체 대상; reorder aboveId null=맨 아래; 잘못된 참조는 Error throw.

- [ ] **Step 1: 실패하는 테스트 작성** — 엔진 테스트 트레이스를 그대로 이식(패리티 보장):

```ts
import { expect, test } from "vitest";
import { buildEntries, opsReducer, OpsState } from "./opsReducer";

const INC = [3, 4, 5];
const ids = (e: { entryId: number }[]) => e.map((x) => x.entryId);

test("no ops keeps order", () => { expect(ids(buildEntries(INC, []))).toEqual([3, 4, 5]); });

test("merge replaces at topmost, sources bottom-to-top", () => {
  const e = buildEntries(INC, [{ op: "merge", layerIds: [5, 3], name: "M" }]);
  expect(ids(e)).toEqual([4, -1]);
  expect(e[1].sourceIds).toEqual([3, 5]);
});

test("merge result can be merged again", () => {
  const e = buildEntries(INC, [
    { op: "merge", layerIds: [3, 4], name: "A" },
    { op: "merge", layerIds: [-1, 5], name: "B" },
  ]);
  expect(ids(e)).toEqual([-2]);
  expect(e[0].sourceIds).toEqual([3, 4, 5]);
});

test("flatten", () => {
  const e = buildEntries(INC, [{ op: "flatten", name: "F" }]);
  expect(ids(e)).toEqual([-1]);
  expect(e[0].sourceIds).toEqual([3, 4, 5]);
});

test("reorder above and to bottom", () => {
  expect(ids(buildEntries(INC, [{ op: "reorder", layerId: 3, aboveId: 5 }]))).toEqual([4, 5, 3]);
  expect(ids(buildEntries(INC, [{ op: "reorder", layerId: 5, aboveId: null }]))).toEqual([5, 3, 4]);
});

test("unknown ref throws", () => {
  expect(() => buildEntries(INC, [{ op: "rename", layerId: 99, name: "x" }])).toThrow();
});

test("undo recomputes entries", () => {
  let s: OpsState = opsReducer(undefined as never, { type: "reset", includedIds: INC });
  s = opsReducer(s, { type: "pushOp", op: { op: "flatten", name: "F" } });
  expect(ids(s.entries)).toEqual([-1]);
  s = opsReducer(s, { type: "undo" });
  expect(ids(s.entries)).toEqual([3, 4, 5]);
});
```

- [ ] **Step 2: 실패 확인** — `npm run test`
- [ ] **Step 3: 구현** — 엔진 `ops.py`의 build_export_plan을 TS로 이식(rename은 entry.name 설정; exclude는 setIncluded로 대체되므로 buildEntries에서 미지원 op으로 throw).
- [ ] **Step 4: 통과 확인** — `npm run test`
- [ ] **Step 5: 커밋** — `feat(app): ops reducer mirroring engine export-plan semantics`

---

### Task 5: 앱 셸 + 파일 패널 + 레이어 트리

**Files:**
- Create: `src/components/FilePanel.tsx`, `src/components/LayerTree.tsx`, `src/components/ErrorPanel.tsx`, `src/state/appStore.tsx` (React context + useReducer)
- Modify: `src/App.tsx`, `src/App.css`
- Test: `src/state/appStore.test.ts` (파일 목록/세션 상태 전이 로직)

**Interfaces:**
- appStore 상태: `files: {path, status: "idle"|"open"|"processing"|"done"|"error", sessionId?, tree?, width?, height?}[]`, `activePath`, `ops: OpsState`(per active file), `matchedIds: number[]`, `errors: {title, error: EngineError}[]`.
- FilePanel: 드래그&드롭(HTML5 drop, `@tauri-apps/plugin-dialog` open()으로도 추가), 파일 클릭 → openPsd → 트리 로드, 상태 뱃지.
- LayerTree: 트리 렌더(그룹 접기/펴기), 각 리프: 체크박스(포함=includedIds), 눈 아이콘(previewHiddenIds), 매치 하이라이트(matchedIds), Shift/Cmd 다중 선택, 우클릭 컨텍스트 메뉴 — 선택 병합(이름 입력), 이름변경, 제외. 그룹 행은 체크박스 없음(접기 전용) + 눈은 하위 전체 토글.
- ErrorPanel: errors 스택 표시, traceback `<pre>` 전문 + 닫기. **엔진 에러는 어디서든 이 패널로**.

**동작 계약:**
- openPsd 성공 시: includedIds 초기값 = 모든 pixel 리프 id(오름차순), previewHiddenIds 초기값 = 원본에서 visible=false인 리프.
- 트리 다중 선택 → 병합 시 `pushOp {op:"merge", layerIds:[선택 리프 id들], name:입력값}`; buildEntries가 throw하면 ErrorPanel에.

- [ ] **Step 1: appStore 전이 테스트 작성** (파일 추가/중복 방지/open 성공·실패 전이/에러 스택 push) → 실패 확인
- [ ] **Step 2: appStore 구현 + 테스트 통과**
- [ ] **Step 3: 컴포넌트 구현** — 스타일은 App.css에 간결한 다크 테마(3-컬럼 grid 레이아웃: 240px | 1fr | 320px, 하단 스트립 160px). 트리 행은 가상화 없이 렌더(수백 노드 수준).
- [ ] **Step 4: 검증** — `npm run test && npm run build` 통과. 수동 확인 포인트를 보고서에 기록(컨트롤러가 tauri dev로 확인).
- [ ] **Step 5: 커밋** — `feat(app): shell layout, file panel, layer tree, error panel`

---

### Task 6: 미리보기 캔버스 + 썸네일

**Files:**
- Create: `src/components/PreviewCanvas.tsx`, `src/lib/preview.ts`
- Modify: `src/components/LayerTree.tsx` (행 썸네일), `src/App.tsx`
- Test: `src/lib/preview.test.ts`

**Interfaces:**
- `src/lib/preview.ts`: `visibleIdsForPreview(tree, includedIds, previewHiddenIds): number[]` — 포함 && 미리보기 켜짐인 리프 id (병합은 미리보기에 영향 없음 — 합성 결과 동일). vitest로 케이스 검증.
- PreviewCanvas: activePath 변경/눈 토글/포함 토글 시 400ms 디바운스로 `renderPreview(sessionId, visibleIds, 1500)` → `loadPngDataUrl` → `<img>`. 줌(휠, 0.1~8배)/팬(드래그) CSS transform, 체커보드 배경(CSS gradient), 로딩 스피너, 렌더 실패는 ErrorPanel로.
- 썸네일: 트리 최초 로드 후 백그라운드로 `renderThumbnails(sessionId, 리프 ids, 48)` 1회 → 행 아이콘. 실패는 ErrorPanel(썸네일 없이 이름만 표시 유지).

- [ ] **Step 1: preview.ts 테스트 작성 → 실패 확인**
- [ ] **Step 2: 구현 + 통과**
- [ ] **Step 3: 캔버스/썸네일 컴포넌트 구현**
- [ ] **Step 4: `npm run test && npm run build` 통과 확인**
- [ ] **Step 5: 커밋** — `feat(app): debounced preview canvas and layer thumbnails`

---

### Task 7: 프리셋 바 + 규칙 편집 + 저장

**Files:**
- Create: `src/components/PresetBar.tsx`, `src/components/PresetDialog.tsx`, `src/lib/presets.ts`
- Modify: `src/App.tsx`
- Test: `src/lib/presets.test.ts`

**Interfaces:**
- `src/lib/presets.ts`: `loadPresets(): Promise<Preset[]>` / `savePresets(list): Promise<void>` — plugin-fs로 `appDataDir()/presets.json` 읽기/쓰기(디렉터리 없으면 생성). 기본 프리셋 1개 내장:

```ts
export const DEFAULT_PRESET: Preset = {
  name: "line 추출",
  include: { type: "contains", value: "line", caseSensitive: false },
  excludeGroupPrefixes: ["-"], matchGroups: true, includeHidden: true,
  merge: "none", naming: "pathPrefix", outputSuffix: "_LINE", embedPreview: true,
};
```

  테스트는 fs 모듈 vi.mock으로 파일 IO 대체(JSON round-trip, 손상 JSON은 throw — 흡수 금지).
- PresetBar: 프리셋 select, "적용"(→ `applyPreset` 호출 → matchedIds 하이라이트 + includedIds=matched + ops=엔진 생성 operations), "편집", "저장/다른 이름으로".
- PresetDialog: 스펙 5절 스키마 전 필드 편집 폼(텍스트/체크박스/select), 유효성(빈 value 금지, regex는 new RegExp try — 실패 메시지 표시).

- [ ] **Step 1: presets.ts 테스트 작성 → 실패 확인**
- [ ] **Step 2: 구현 + 통과**
- [ ] **Step 3: UI 구현 (적용 흐름 포함)**
- [ ] **Step 4: `npm run test && npm run build`**
- [ ] **Step 5: 커밋** — `feat(app): preset bar, rule editor, appdata persistence`

---

### Task 8: 작업 히스토리 + 내보내기 다이얼로그

**Files:**
- Create: `src/components/OpsHistory.tsx`, `src/components/ExportDialog.tsx`
- Modify: `src/App.tsx` (툴바 내보내기 버튼)
- Test: `src/lib/exportFlow.test.ts`

**Interfaces:**
- OpsHistory(하단 스트립): ops 목록을 사람이 읽는 문장으로("병합: line, lines → M"), 마지막 항목 취소(undo) 버튼.
- ExportDialog:
  - entries 목록 표시(아래→위), 각 행: 이름(merge명 또는 원본 레이어명), HTML5 drag&drop으로 순서 변경 → `pushOp {op:"reorder", layerId: entryId, aboveId: ...}`, 더블클릭 인라인 rename → `pushOp {op:"rename", ...}`.
  - (스펙 6절의 "트리 드래그 순서변경"에 대한 의도된 편차: 순서는 평탄한 내보내기 목록의 속성이므로 원본 구조를 보존하는 트리가 아니라 이 목록에서 드래그한다 — 스펙 문서에 반영됨)
  - naming 라디오(pathPrefix/original), embedPreview 체크박스.
  - "내보내기" → `save({ defaultPath: 원본폴더/원본stem+outputSuffix+".psd", filters:[{name:"Photoshop", extensions:["psd"]}] })` (plugin-dialog) → 취소면 중단 → `exportPsd(sessionId, state.includedIds, state.ops, naming, outputPath, embedPreview, overwrite: true, verify: true)` (includedIds는 체크박스 상태 그대로, operations는 히스토리의 ops 그대로 — 엔진이 동일한 build_export_plan을 서버측에서 수행).
    - 주의: save 다이얼로그가 이미 덮어쓰기 확인을 하므로 overwrite:true로 호출.
  - progress: `onEngineEvent`로 stage/current/total 표시(프로그레스 바).
  - 결과: verification 요약(ok 뱃지, 레이어별 nameOk/pixelOk 테이블, layerCountOk 불일치 시 expected/actual 강조). ok=false면 성공으로 표시하지 않는다. 실패는 ErrorPanel.
- `src/lib/exportFlow.ts`: `defaultExportPath(srcPath, suffix)` (경로 조작 순수 함수) + reorder 대상 계산 `reorderArgs(entries, fromIdx, toIdx): {layerId, aboveId}` — vitest.

- [ ] **Step 1: exportFlow 테스트 작성(defaultExportPath, reorderArgs 경계 포함: 맨 아래로 이동 시 aboveId null) → 실패 확인**
- [ ] **Step 2: 구현 + 통과**
- [ ] **Step 3: 컴포넌트 구현**
- [ ] **Step 4: `npm run test && npm run build`**
- [ ] **Step 5: 커밋** — `feat(app): ops history and export dialog with verification report`

---

### Task 9: 배치 패널

**Files:**
- Create: `src/components/BatchPanel.tsx`, `src/lib/batch.ts`
- Modify: `src/App.tsx` (툴바 배치 버튼/모드)
- Test: `src/lib/batch.test.ts`

**Interfaces:**
- `src/lib/batch.ts`: `planBatchOutputs(paths, outputDir | null, suffix): {path, outputPath}[]` + `findConflicts(planned, existsFn): Promise<string[]>` (plugin-fs exists — 테스트는 existsFn 주입). vitest.
- BatchPanel 흐름:
  1. 파일 목록(FilePanel의 파일 전체 또는 선택), 프리셋 select.
  2. 출력: "각 원본과 같은 폴더" 체크 또는 폴더 선택(`open({directory:true})`).
  3. 실행 전 충돌 검사 → 동명 파일 목록 모달로 보여주고 "덮어쓰기 진행/취소" 일괄 확인 (스펙 5절).
  4. `batchRun(paths, preset, outputDir, overwrite: 사용자확인값)` — 진행률: engine-event의 path/stage 표시.
  5. 결과 테이블: 파일별 ok/레이어 수/출력 경로/실패 시 traceback 펼치기(ErrorPanel 재사용 스타일).
- 주의: batch_run은 엔진이 파일을 자체적으로 열므로 열린 세션과 무관. 실패해도 큐 계속(엔진 보장) — UI는 결과만 표시.

- [ ] **Step 1: batch.ts 테스트 작성 → 실패 확인**
- [ ] **Step 2: 구현 + 통과**
- [ ] **Step 3: 컴포넌트 구현**
- [ ] **Step 4: `npm run test && npm run build`**
- [ ] **Step 5: 커밋** — `feat(app): batch panel with conflict pre-check and per-file results`

---

### Task 10: 엔진 생존성 UI + 통합 마감

**Files:**
- Create: `src/components/EngineStatus.tsx`
- Modify: `src/App.tsx`, 필요한 폴리시 수정 전반 (기능 추가 금지 — 마감만)
- Test: 기존 스위트 전체 + `npm run build`

**Interfaces:**
- EngineStatus: `onEngineDead` 수신 시 상단에 붉은 배너 "엔진 프로세스가 종료되었습니다" + "재시작" 버튼(`invoke("restart_engine")`, 성공 시 활성 파일 세션 재-open 필요 안내 — 세션은 소멸됨을 명시하고 파일 목록 상태를 idle로 리셋).
- 마감 체크리스트(각각 확인 후 보고서에 기록):
  - 모든 엔진 호출 경로가 실패 시 ErrorPanel로 traceback 노출하는가 (grep으로 catch 블록 전수 확인 — 삼키는 catch 금지)
  - 파일 닫기/전환 시 close_session 호출
  - 앱 종료 시 Rust가 엔진 child kill
  - vitest 전체 + `cargo test` + `npm run build` 통과
- 수동 E2E 절차서를 보고서에 포함(컨트롤러 실행용): 대용량 실파일 열기 → 'line 추출' 프리셋 적용 → 미리보기 확인 → 병합 1회 → 내보내기(저장 다이얼로그) → 검증 ok 확인 → 배치 2파일 실행.

- [ ] **Step 1: EngineStatus 구현**
- [ ] **Step 2: 마감 체크리스트 수행 + 발견 사항 수정**
- [ ] **Step 3: 전체 검증** — `npm run test && npm run build && (cd src-tauri && cargo build && cargo test)`
- [ ] **Step 4: 커밋** — `feat(app): engine liveness UI and integration polish`
